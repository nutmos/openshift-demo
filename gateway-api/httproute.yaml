apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: httpd
spec:
  parentRefs:
  - name: default-gateway
  hostnames:
  - "httpd.greeter.cluster-bcg8l.bcg8l.sandbox1255.opentlc.com"
  rules:
  - backendRefs:
    - name: httpd
      port: 8080
