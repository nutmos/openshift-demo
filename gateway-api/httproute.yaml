apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: httpd
spec:
  parentRefs:
  - name: default-gateway
  hostnames:
  - "httpd.greeter.${CLUSTER_NAME}"
  rules:
  - backendRefs:
    - name: httpd
      port: 8080
